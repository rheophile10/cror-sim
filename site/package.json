{
  "name": "cror-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "cror.ca: a menu over the simulator and the conductor game.",
  "scripts": {
    "pack": "node pack.mjs",
    "build": "node build.mjs",
    "test": "node test/site.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.62.0"
  }
}
